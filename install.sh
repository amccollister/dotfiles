# For Arch/pacman based systems only
# Needs to be written still but the most important command is:
#
# stow -t ~ dotfiles/*
# This will install everything at once. Need to work on overwriting the existing files as well as installed all the necessary dependencies
